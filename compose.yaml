services:
  postgresql:
    image: postgres:15
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_DB=metrics
      - POSTGRES_PASSWORD=userpassword
      - POSTGRES_USER=metrics
    volumes:
      - postgresql:/var/lib/postgresql:rw
volumes:
  postgresql:
